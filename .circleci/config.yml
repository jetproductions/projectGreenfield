 version: 2
 jobs:
   build:
     docker:
       - image: circleci/node:12.0.0
     steps:
      - checkout
      - run: 
        name: update-npm
        command: 'npm install -g npm@latest'
        