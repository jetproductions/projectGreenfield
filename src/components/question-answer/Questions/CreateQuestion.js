import React from 'react';

const submitQuestion = (text, id) => {
  fetch();
};

const CreateQuestion = ({ toggleModal }) => {


  // after submit could put timeer on submitted then close modal
};

export default CreateQuestion;
